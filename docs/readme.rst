.. include:: ../README.rst

run server code and then client the code.
the client code will control user database and collect all the data that the server needs in order to run the experiment.

notice that the csv is saved to log file. every run runs-over the last use. currently the user needs to manually place the
file in the relevant place. -- to be solved in future versions.

client - notice that the sensor identity need to be typed correctly (caps).
